server:
  port: 8080

spring:
  application:
    name: rag-ingestion

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

app:
  text-chunker:
    chunk-size: 512
    overlap: 100